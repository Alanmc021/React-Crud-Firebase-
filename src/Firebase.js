import firebase from "firebase";

var firebaseConfig = {
    //Configuração do Firebase aqui meu amigo dev !!! 
  };
  // Initialize Firebase
  firebase.initializeApp(firebaseConfig);
  
  export default firebase